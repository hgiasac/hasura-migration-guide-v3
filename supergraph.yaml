kind: Supergraph
version: v2
definition:
  subgraphs:
    - globals/subgraph.yaml
    - data/subgraph.yaml
    - chinook/subgraph.yaml
    - app/subgraph.yaml
    # TODO: uncomment after the stack overflow bug was fixed
    # - countries/subgraph.yaml
